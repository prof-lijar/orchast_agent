# Installation

This page provides instructions on how to install and run `orchast_agent` locally.

## Prerequisites
- Python 3.x
- [List other dependencies]

## Setup Steps
1. Clone the repository:
   ```bash
   git clone https://github.com/user/orchast_agent.git
   ```
2. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```
3. Run the application:
   ```bash
   python main.py
   ```
